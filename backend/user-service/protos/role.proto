syntax = "proto3";

service UserService {
  rpc GetAllRoles (Empty) returns (RoleList);
}

message Empty {}

message Role {
  string id = 1;
  string role = 2;
}

message RoleList {
  repeated Role roles = 1;
}
